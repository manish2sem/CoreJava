package DS.List.stack.JavaBuiltinSupport;

import java.util.Stack;
import java.util.Vector;
import java.util.concurrent.ArrayBlockingQueue;;

/**
 * Defined generically in java.util package.
 * Extends Vector.
 * 	class Stack<E> extends Vector<E>
 * 
 * Just one constructor(like default)
 *  public Stack() {
    }
    
    Supports following functios :
    
    public E push(E item)
    public synchronized E pop() 
    public synchronized E peek() 
    public boolean empty()
    public synchronized int search(Object o) 
    		returns the distance from the top of the stack of the occurrence of object.
    		The equals method is used to compare objects to the items in this stack.
    		the return value -1 indicates that the object is not on the stack
    		the return value 1 indicates that the object is at the top of the stack
    
 * @author sinhama
 *
 */


public class JavaStack {
	public static void main(String args[] ) {
		Stack<String> myStack = new Stack<String>();
	}

}
