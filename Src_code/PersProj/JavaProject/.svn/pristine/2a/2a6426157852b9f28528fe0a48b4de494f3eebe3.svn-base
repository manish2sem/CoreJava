package CoreJava.J_Elementry.VarLengthArg;

/**
 * Arguments 
 * 	- The things you specify between the parentheses when you're invoking a method: 
 * 		doStuff("a", 2); // invoking doStuff, so a & 2 are arguments
 * Parameters 
 * 	- The things in the method's signature that indicate what the method must receive when it's invoked:
 * 		void doStuff(String s, int a) { } // we're expecting two parameters: String and int

 * declaration rules for var-args:
 * 	- you can have only one var-arg in a method.
 * 	- It's legal to have other parameters in a method that uses a var-arg. 
 * 	- The var-arg must be the last parameter in the method's signature, if it has other parameters
 *  
 */

public class VarArgsContracts {

}
